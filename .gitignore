# Build output — regenerate with `npm run build`
dist/

# Dependencies (this project has none, but keep the guard)
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Secrets — never commit. DATABASE_URL lives in Vercel env vars only.
.env
.env.*
!.env.example
